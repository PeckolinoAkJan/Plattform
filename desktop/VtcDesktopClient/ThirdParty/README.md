# RenCloud SCS SDK telemetry

This directory contains the pinned RenCloud `scs-sdk-plugin` release 1.12.1 assets used by the desktop client and installer.

Upstream: https://github.com/RenCloud/scs-sdk-plugin/releases/tag/V.1.12.1

The upstream license and notices shipped inside the release archive must remain with distributed binaries.
